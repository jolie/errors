service HelloService {
    inputPort HelloService {
    }
} // For some reason '??' is printed after this curly bracket, when no extra line or space is added
// Still need to find the correct line to print